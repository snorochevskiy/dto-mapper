buildscript {
    repositories {
        jcenter()
    }
    dependencies {
 
    }
}

repositories{
    jcenter()
}

apply plugin: 'java'


dependencies {
    compile group: 'org.ow2.asm', name: 'asm-all', version: '5.2'

    compile group: 'junit', name: 'junit', version: '4.12'

}
